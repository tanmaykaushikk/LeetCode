 // sum 
        int sum = 0;
        for(int i=0;i<nums.length;i++){
            sum = sum + nums[i];
        }
        // actual sum 
        int n = nums.length;

        int as = n*(n+1) / 2;

        int ans = as - sum;

        return ans;
